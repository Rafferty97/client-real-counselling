extends ../../base/views/base

block title
  title Real Counselling - Annie Winton

block content
  each current, index in segments
    != renderFile(current.view, {current: current, index: index, segments: segments})
